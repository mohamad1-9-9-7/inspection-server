/* Sweets industry — its own training sessions (pages/sweets-training in the
   frontend), never mixed with the meat company's training_session rows. */
module.exports = { sessionType: "sweets_training_session" };
