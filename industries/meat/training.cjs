/* Meat industry — the report type its training sessions are stored under.
   The public quiz-by-token routes accept only the session types declared by
   the industries (industries/index.cjs → TRAINING_SESSION_TYPES). */
module.exports = { sessionType: "training_session" };
